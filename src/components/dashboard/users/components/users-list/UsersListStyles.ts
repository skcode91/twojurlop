import styled from "@emotion/styled";
import { TableCell } from "@mui/material";

export const UlMainContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 24px;
  padding: 24px 0;
`;

export const UlExpandedContainer = styled.div`
  display: flex;
  width: 100%;
  gap: 24px;
  padding: 24px 0;
  background: lightgray;
`;
