import styled from "@emotion/styled";
import { Paper } from "@mui/material";

export const CaMainContainer = styled(Paper)`
  display: flex;
  flex-direction: column;
  gap: 24px;
  padding: 24px;
  background: #1dcaff;
  margin: 24px 0;
`;

export const CaItem = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0px;
`;
